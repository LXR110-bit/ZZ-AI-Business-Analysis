[project]
name = "lark-tools"
version = "0.1.0"
description = "MCP server: lark-cli wrapper"
requires-python = ">=3.11"
dependencies = ["mcp>=1.6.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lark_tools"]
