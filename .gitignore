# Secrets (NEVER commit)
.env
.env.local
secrets/
*.key
*.pem

# Python
__pycache__/
*.py[cod]
*.so
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Node
node_modules/

# Codex / Lark CLI local state
.codex/
.lark-cli/

# Logs
logs/
*.log

# Cache
.cache/
*.tmp

# OS
.DS_Store
Thumbs.db

# Agent 协作 pad(见 docs/superpowers/handoffs/master_agent_bootstrap.md §六)
data/agent_notes/

# feishu_push outbox 落盘目录(dry-run / 兜底,不入库)
tools/feishu_push/outbox/

# monitor_lib_shared 运行时产物
data/monitor_output/
data/outbox/

# 等价性验证快照(生产敏感数据,可复现,不入库)
# verify 脚本(scripts/verify_equivalence_real.py)本身入库,数据不入
data/real_snapshot/
